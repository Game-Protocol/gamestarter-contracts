var timestamp = require('../scripts/timestamp');

var BackgammonToken = artifacts.require("BackgammonToken");
var Wagering = artifacts.require("Wagering");

module.exports = async deployer => {

  // await deployer.deploy(BackgammonToken);
  // console.log("BackgammonToken Address: " + BackgammonToken.address);
  // var types = ["address"];
  // var params = [BackgammonToken.address];
  // verifyCode.toABI("Wagering.abi.txt", types, params);
  // deployer.deploy(Wagering, BackgammonToken.address);
  // await console.log("Wagering Address: " + Wagering.address);
};